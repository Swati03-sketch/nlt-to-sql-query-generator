# nlt-to-sql-query-generator
A Text-to-SQL Query Generator allows users to enter natural language text, and the system converts it into an executable SQL query. It generates SQL queries using NLP and Machine Learning techniques, leveraging libraries and models such as T5-small and NLTK. SQLite is used as the database for training, testing, and validating the generated queries.
